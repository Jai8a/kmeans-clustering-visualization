# Iris Clustering Using K-Means

This project implements K-Means clustering using scikit-learn to group Iris flower samples based on their morphological features.

The objective is to analyze natural data groupings and determine the optimal number of clusters using the Elbow Method.
